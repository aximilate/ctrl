services:
  ctrlchat-api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: ctrlchat-api
    env_file:
      - ./server/.env
    ports:
      - "8080:8080"
    volumes:
      - ./server/data:/app/data
    restart: unless-stopped
